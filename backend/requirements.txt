# ── AppSail deployment requirements ──────────────────────────────────────────
# Minimal set — no heavy ML libs that cause startup timeouts or build failures.
# sentence-transformers → removed (downloads 90MB model; TF-IDF fallback used)
# faiss-cpu → removed (native C++ build; sklearn cosine fallback used)
# psycopg2-binary → removed (PostgreSQL-only; using SQLite on AppSail)
# geoalchemy2, pgvector → removed (PostgreSQL extensions)
# celery, redis → removed (async workers not used in sync mode)
# kagglehub, pytest → removed (dev-only)

fastapi>=0.110.0
uvicorn>=0.28.0
sqlalchemy>=2.0.28
pandas>=2.2.1
numpy>=1.26.4
scikit-learn>=1.4.1.post1
networkx>=3.2.1
statsmodels>=0.14.1
jinja2>=3.1.3
python-dotenv>=1.0.1
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
requests>=2.31.0
pydantic>=2.6.4
pydantic-settings>=2.2.1
zcatalyst-sdk
slowapi>=0.1.9
pyotp>=2.9.0
